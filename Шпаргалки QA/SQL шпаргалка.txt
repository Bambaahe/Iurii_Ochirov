SELECT {{какой столбик вывести}}
FROM {{из какой таблички}}
JOIN {{какую таблицу добавляем}}
ON {{название столбика из одной таблицы}} = {{названию cтолбика из другой}}
WHERE {{какой столбик известен из текста вопроса}} = {{его значение}}
ORDER BY {{числовой столбец, по которому сортировать}} по умолчанию по возрастанию. По убыванию + DESC
LIMIT {{число, ограничение выборки}} //LIMIT - правило хорошего тона, чтобы по неосторожности не повесить БД.    
SELECT COUNT(*) [возвращаает количество строк]   
UPDATE {{название таблицы}} SET {{какой столбик меняем}} = {{новое значение}}